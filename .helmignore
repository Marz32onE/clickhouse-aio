# Patterns to ignore when building packages.
# VCS
.git/
.gitignore
# Test / session artifacts
.playwright-mcp/
*.png
*.log
dist/
# OS / IDE
.DS_Store
.idea/
.vscode/
*.swp
*~
# CI / docs not needed inside the package
Makefile
